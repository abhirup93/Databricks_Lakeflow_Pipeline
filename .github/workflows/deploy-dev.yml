name: Deploy — Dev

on:
  push:
    branches:
      - dev
    paths:
      - 'Databricks_Lakeflow_Pipeline/resources/**'
      - 'Databricks_Lakeflow_Pipeline/databricks.yml'
      - 'wanderbricks_dlt_pipeline/**'

jobs:
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    environment: dev

    # ↓ This tells every step to run from this subfolder automatically
    # No more cd in every step
    defaults:
      run:
        working-directory: Databricks_Lakeflow_Pipeline

    concurrency:
      group: deploy-dev
      cancel-in-progress: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Databricks CLI
        uses: databricks/setup-cli@main

      - name: Validate bundle
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: databricks bundle validate --target dev

      - name: Deploy to dev
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: databricks bundle deploy --target dev

      - name: Summary
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "✅ Dev deployment complete"
          databricks bundle summary --target dev